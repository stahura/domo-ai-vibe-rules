# Skills Directory

Each subfolder here is one skill with a single entrypoint:

```text
skills/<feature>/<skill-name>/SKILL.md
```

Current feature groups:

- `administration/`
- `apps/`
- `cli/`
- `connectors/`
- `datagen/`
- `documents/`
- `domo-everywhere/`
- `skill-orchestration/`
- `themes/`
- `transformation/`
- `visualization/`

CLI skills currently available:

- `cli/code-engine-create`
- `cli/code-engine-update`
- `cli/appdb-collection-create`

For installation/discovery and the full catalog, use the root `README.md`.
